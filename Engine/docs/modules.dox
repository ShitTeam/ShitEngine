/* ======================================================================
 * modules.dox — 模块分组定义
 *
 * 纯文档文件（不参与编译）：把引擎 15 个模块组织为 Doxygen 组，
 * 左侧导航树据此呈现「模块 → 说明 + 关键类」的结构，而非目录堆叠。
 * 组页内容为手写的职责说明与精选类链接；完整类列表仍在命名空间页。
 * ====================================================================== */

/** @defgroup core 核心框架
 *  @brief 引擎主循环、窗口、时间、日志与子系统上下文。
 *
 * 引擎生命周期由 Game 驱动：`Init()` 完成日志 → 配置 → 反射注册 → SDL →
 * 窗口/渲染/输入/音频的初始化；`Run()` 执行「事件 → 更新 → 渲染」主循环；
 * `Destroy()` 按初始化逆序清理。全部子系统实例由 EngineContext 按值持有，
 * 支持多上下文（编辑器进程内预览即第二个上下文）。
 *
 * 关键类：
 * - @ref Shit::Game "Game" —— Init / Run / Destroy / SetPaused
 * - @ref Shit::EngineContext "EngineContext" —— 子系统容器（多上下文）
 * - @ref Shit::Window "Window" / @ref Shit::Time "Time" / @ref Shit::Log "Log" / @ref Shit::Config "Config"
 * - @ref Shit::System "System" —— 场景系统基类（优先级排序驱动）
 */

/** @defgroup gameobj 游戏对象与组件
 *  @brief 组件容器模型与引擎内置组件。
 *
 * GameObject 是组件容器：`addComponent<T>()` 保证每种组件单实例，
 * `getComponent<T>()` 按 type_index O(1) 查找。组件生命周期：
 * onCreate → onAttach（挂入场景）→ onDetach → onDestroy。
 * 对象支持父子层级、tag 标签与 active 启用态（isActiveInHierarchy
 * 随父链级联，失活对象不渲染/不更新/不参与物理）。
 *
 * 关键类：
 * - @ref Shit::GameObject "GameObject" —— 组件容器与层级
 * - @ref Shit::Component "Component" —— 组件基类（持久 UUID）
 * - 内置组件：@ref Shit::TransformComponent "TransformComponent"、
 *   @ref Shit::SpriteRenderer "SpriteRenderer"、@ref Shit::CameraComponent "CameraComponent"、
 *   @ref Shit::Tilemap "Tilemap"
 * - 跨对象引用：@ref Shit::ComponentRef "ComponentRef"（存 UUID 懒解析，目标销毁自动失效）
 */

/** @defgroup scene 场景管理
 *  @brief 场景持有、切换与 .scene 数据驱动加载。
 *
 * 单一场景模型：SceneManager 持有当前活跃场景，LoadScene 同帧切换。
 * 场景以 JSON 格式的 `.scene` 文件为唯一事实标准——编辑器保存、运行时
 * 加载、切关共用同一序列化器。结构变更通过代数（generation）通知编辑器同步。
 *
 * 关键类：
 * - @ref Shit::Scene "Scene" —— 对象容器 / 延迟增删 / 组件 UUID 索引
 * - @ref Shit::SceneManager "SceneManager" —— 当前场景与切换
 * - @ref Shit::SceneSerializer "SceneSerializer" —— .scene 读写（含相机兜底）
 * - @ref Shit::Prefab "Prefab" —— 对象捕获 / 实例化（复制粘贴与预置体基础）
 */

/** @defgroup behavior 行为脚本
 *  @brief 用户游戏逻辑入口（类似 Unity MonoBehaviour）。
 *
 * 继承 Behavior 实现 onStart()/onUpdate()，挂到 GameObject 后由
 * BehaviorSystem 自动驱动；物理接触回调 onCollisionEnter/Stay/Exit
 * 提供碰撞响应点。全局暂停（Game::SetPaused）冻结 onUpdate 但保留渲染。
 *
 * 关键类：
 * - @ref Shit::Behavior "Behavior" —— 脚本基类
 * - @ref Shit::BehaviorSystem "BehaviorSystem" —— 驱动系统
 */

/** @defgroup render 渲染
 *  @brief 多相机管线、精灵绘制与离屏渲染。
 *
 * RenderSystem 按 zIndex 排序逐相机绘制，相机可定义独立视口比例
 * （分屏 / letterbox 等比缩放）。逻辑分辨率固定，窗口缩放不影响坐标系。
 *
 * 关键类：
 * - @ref Shit::RenderSystem "RenderSystem" —— 相机遍历与绘制调度
 * - @ref Shit::Renderer "Renderer" —— SDL 渲染门面（离屏目标 / 视口 / 绘制原语）
 * - @ref Shit::Sprite "Sprite" —— 精灵（纹理 + 变换 + 源矩形）
 * - @ref Shit::SpriteSheet "SpriteSheet" —— 精灵表切片数学
 */

/** @defgroup animation 动画
 *  @brief 参数驱动的状态机与帧动画剪辑。
 *
 * Animator 是状态机组件：状态集 + 转换条件（float/bool/trigger 参数驱动）+
 * 每状态引用一份 AnimationClip；剪辑描述精灵表网格切片与每帧时长，
 * 可作为 .anim 资产独立保存。两者均随 .scene 序列化。
 *
 * 关键类：
 * - @ref Shit::Animator "Animator" —— 状态机组件
 * - @ref Shit::AnimationClip "AnimationClip" —— 可序列化帧动画剪辑
 * - @ref Shit::AnimationComponent "AnimationComponent" —— 多剪辑播放组件
 */

/** @defgroup audio 音频
 *  @brief 分层增益（master × group × track）的音频播放。
 *
 * AudioPlayer 管理播放轨道并自动回收已播完的 track；AudioSource 组件
 * 在场景内声明音频并可随场景序列化。底层经 ResourceManager 缓存 MIX_Audio。
 *
 * 关键类：
 * - @ref Shit::AudioPlayer "AudioPlayer" —— 播放门面与轨道管理
 * - @ref Shit::AudioSource "AudioSource" —— 场景音频组件
 * - @ref Shit::AudioTrack "AudioTrack" —— 单次播放句柄
 */

/** @defgroup input 输入
 *  @brief 键鼠三态查询与动作/轴映射。
 *
 * 两层 API：底层 KeyCode/MouseButton 三态查询（Down 仅一帧 / Pressed 持续 /
 * Released 松开一帧）；上层动作映射（settings.json 的 inputMappings 配置，
 * 编辑器项目设置可视化编辑），用语义名（"Jump"/"Horizontal"）解耦具体按键。
 *
 * 关键类：
 * - @ref Shit::Input "Input" —— 全静态门面
 * - @ref Shit::KeyCode "KeyCode" / @ref Shit::MouseButton "MouseButton" —— 键码表
 */

/** @defgroup event 事件
 *  @brief 缓冲队列事件总线。
 *
 * Emit() 入队、ProcessEvents() 在主循环固定时刻统一派发；回调内可安全
 * 增删订阅者，避免递归派发与迭代器失效。
 *
 * 关键类：
 * - @ref Shit::EventBus "EventBus" —— 订阅 / 发布
 * - @ref Shit::Event "Event" —— 事件结构基类
 */

/** @defgroup resource 资源
 *  @brief 纹理 / 字体 / 音频的统一加载与缓存。
 *
 * Resource 基类封装路径、加载状态与会话级 uuid；ResourceCache 模板统一
 * 「查缓存 → 懒加载 → RAII 回收」。调用方借用裸句柄（SDL_Texture* 等），
 * 缓存在 Game::Destroy 统一按序销毁。相对路径经资产根解析（SetAssetRoot）。
 *
 * 关键类：
 * - @ref Shit::ResourceManager "ResourceManager" —— 门面（GetTexture/GetFont/GetAudio）
 * - @ref Shit::Texture "Texture" / @ref Shit::Font "Font" / @ref Shit::Audio "Audio" —— 资源封装
 */

/** @defgroup physics 物理
 *  @brief Box2D 3.x 封装的 2D 物理系统。
 *
 * 以像素为长度单位（无需手动换算）；刚体 + 碰撞体 + 关节声明物理结构，
 * 接触事件驱动 Behavior 的碰撞回调（回调内可安全销毁对象）。首个刚体
 * 挂载时自动注册物理系统（自愈机制，数据驱动场景无需手动配置）。
 *
 * 关键类：
 * - @ref Shit::PhysicsSystem2D "PhysicsSystem2D" —— 世界步进与接触派发
 * - @ref Shit::RigidBody2D "RigidBody2D" / @ref Shit::BoxCollider2D "BoxCollider2D" /
 *   @ref Shit::CircleCollider2D "CircleCollider2D" / @ref Shit::Joint2D "Joint2D"
 */

/** @defgroup ui UI 系统
 *  @brief Canvas / 控件体系（独立于游戏渲染管线）。
 *
 * UICanvas 为 UI 根节点，控件按 UI 坐标空间绘制并参与射线拾取
 * （按钮点击等交互）。失活对象的 UI 不绘制、不响应点击。
 *
 * 关键类：
 * - @ref Shit::UICanvas "UICanvas" —— UI 渲染根
 * - @ref Shit::UIText "UIText" / @ref Shit::UIButton "UIButton" /
 *   @ref Shit::UITextInput "UITextInput" —— 常用控件
 * - @ref Shit::UIRenderSystem "UIRenderSystem" —— 绘制 + 射线 + 按钮状态更新
 */

/** @defgroup reflection 反射
 *  @brief 运行时类型信息：字段/方法/属性元数据与代码生成。
 *
 * 用宏标记类型与成员，ReflectionScanner（libClang）在构建期扫描生成
 * .gen.h 注册代码，运行时 TypeRegistry 提供按名查找、字段读写与工厂创建
 * ——编辑器属性面板、.scene 序列化、Prefab 复制粘贴均构建于此。
 *
 * 关键类：
 * - @ref Shit::TypeRegistry "TypeRegistry" —— 类型表（注册 / 查询）
 * - @ref Shit::TypeInfo "TypeInfo" —— 字段/方法/属性/枚举元数据
 * - 反射宏（SHIT_REFLECT / SHIT_FIELD / SHIT_METHOD / SHIT_PROPERTY）见 Macros.h，内含用法示例
 */

/** @defgroup plugin 插件
 *  @brief 脚本 DLL 动态加载（Runtime 与编辑器共用）。
 *
 * 插件 = 脚本库：只导出身份信息 + RegisterPluginTypes 注册反射类型，
 * 不含场景搭建代码。Runtime 与编辑器从 config.json 加载插件后即可
 * 实例化自定义行为并参与序列化。
 *
 * 关键类：
 * - @ref Shit::PluginManager "PluginManager" —— 加载 / 卸载 / 类型注册
 */

/** @defgroup math 数学
 *  @brief 向量别名与矩形。
 *
 * Vector2 为 glm::vec2 别名（引擎以像素为单位）；Rect 为浮点矩形，
 * 与 SDL_FRect 可互转。
 *
 * 关键类：
 * - @ref Shit::Rect "Rect"
 */
