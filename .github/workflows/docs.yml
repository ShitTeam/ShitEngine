# GitHub Actions — API 文档自动构建与部署
# 引擎推送到 main 时自动生成 Doxygen 文档并推送到文档网站仓库的
# docs/public/api/ 目录，VitePress 自动复制到输出根目录 /api/
name: Deploy API Docs

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安装 Doxygen + SDL3 构建依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen cmake g++ \
            libx11-dev libxext-dev libxrandr-dev libxcursor-dev \
            libxfixes-dev libxi-dev libxss-dev libxtst-dev \
            libwayland-dev libegl1-mesa-dev libgles2-mesa-dev libgl1-mesa-dev \
            libxkbcommon-dev libdbus-1-dev \
            libpulse-dev libpipewire-0.3-dev \
            libdrm-dev libgbm-dev

      - name: CMake 配置
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug

      - name: 生成 API 文档
        run: cmake --build build --target docs

      - name: 推送到 ShitEngine-Docs
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.API_DOCS_TOKEN }}
          external_repository: ShitTeam/ShitEngine-Docs
          publish_dir: ./build/docs/api
          destination_dir: docs/public/api
          publish_branch: main
